# Test fixtures

Place the mock data image here to run image-parser tests against real image data:

- **mock_data_table.png** – Screenshot of the Week / Work Points table (or copy the attached image from `assets/mock_data-*.png` if present).

If the file is missing, tests that require it are skipped.
