# Text-to-Chart Environment Configuration
# ---------------------------------------
# 1. Copy this file to .env in the project root:
#      cp .env.example .env
# 2. Edit .env and set your values (e.g. OPENAI_API_KEY).
# 3. The app loads .env automatically when you run the API, Streamlit UI, Developer Portal, or CLI.

# ---------------------------------------------------------------------------
# LLM (OpenAI) – required for AI chart suggestions and image-to-table
# ---------------------------------------------------------------------------
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: override models (defaults shown)
LLM_MODEL=gpt-4o-mini
VISION_MODEL=gpt-4o

# ---------------------------------------------------------------------------
# API (REST server)
# ---------------------------------------------------------------------------
# Comma-separated API keys. Leave empty for dev (no auth required).
API_KEYS=

# Rate limit per API key (e.g. 60/minute, 100/hour)
RATE_LIMIT=60/minute

API_HOST=0.0.0.0
API_PORT=8000

# ---------------------------------------------------------------------------
# Logging (for debugging image parse / chart flow)
# ---------------------------------------------------------------------------
# INFO = one-line [DEBUG] messages; DEBUG = verbose
# LOG_LEVEL=DEBUG

# ---------------------------------------------------------------------------
# Streamlit Web UI
# ---------------------------------------------------------------------------
UI_HOST=0.0.0.0
UI_PORT=5001

# API base URL for Developer Portal and Admin UI (when API runs elsewhere)
# API_BASE_URL=http://localhost:8000

# ---------------------------------------------------------------------------
# Storage
# ---------------------------------------------------------------------------
CHART_TTL_HOURS=24
USAGE_DB_PATH=usage.db
SAAS_DB_PATH=saas.db

# ---------------------------------------------------------------------------
# SaaS (Developer Portal, Admin UI)
# ---------------------------------------------------------------------------
# JWT secret for account tokens (change in production)
JWT_SECRET=change-me-in-production

# Admin credentials for Admin UI
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# ---------------------------------------------------------------------------
# Chart defaults
# ---------------------------------------------------------------------------
CHART_TEMPLATE=plotly_white
